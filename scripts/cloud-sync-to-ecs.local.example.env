# 复制为 cloud-sync-to-ecs.local.env（已被 .gitignore 忽略）后填写
# 使用: set -a && source scripts/cloud-sync-to-ecs.local.env && set -a && ./scripts/cloud-sync-to-ecs.sh

ECS_SYNC_HOST=47.114.111.216
ECS_SYNC_USER=root
ECS_SYNC_REMOTE_PATH=/opt/hemodialysis
